# 安卓投屏软件规格说明（云端开发 / Windows EXE / 开源）

## 一、目标
- **投屏到电脑**：手机端不开发独立 App，尽量依赖系统能力与 ADB。
- **Windows EXE 便携版**：降低安装门槛，适合在他人电脑直接使用。
- **兼容多版本 Android**：覆盖常见机型与系统版本。
- **全流程云端构建**：CI/CD 产出安装包/便携 EXE，减少本地依赖。

## 二、功能清单
### 1) 投屏与控制
- 实时显示安卓设备屏幕（低延迟）。
- 鼠标与键盘控制。
- 全屏显示。
- 下拉通知栏/快捷设置（系统级下拉）。
- 支持手机快捷操作（返回、主页、多任务）。
- 投屏后静音手机，断开连接后自动恢复音量。
- 使用电脑输入法在手机上输入。

### 2) 文件与应用
- 拖拽 APK 到视频窗口安装。
- 拖拽文件到视频窗口发送至手机。

### 3) 连接与体验
- USB 与无线两种连接方式。
- 无线连接：自动扫描同网段设备。
- 双向剪贴板同步（电脑 ↔ 手机）。

## 三、技术路线（建议）
### 1) 关键模块
```
Windows 客户端（EXE）
  ├─ 渲染层：显示投屏画面、全屏、拖拽
  ├─ 控制层：鼠标键盘 → Android 输入
  ├─ 连接层：USB / Wi‑Fi
  ├─ 文件与 APK 传输
  └─ 剪贴板同步

Android 侧（无独立 App）
  ├─ 屏幕采集：MediaProjection/系统服务
  ├─ 控制通道：ADB input/无障碍服务
  ├─ 文件安装：ADB install + push
  └─ 剪贴板桥接：ADB/系统接口
```

### 2) 推荐技术
- **采集编码**：MediaProjection + H.264（MediaCodec）。
- **传输**：WebRTC 或自定义低延迟 TCP/UDP。
- **控制**：USB（ADB）、无线（ADB over Wi‑Fi 或独立控制通道）。
- **Windows 客户端**：Electron 或 .NET/WPF，解码可用 FFmpeg。
- **打包**：单文件/便携 EXE。

## 四、开源许可建议
- **不是“越新越好”**，而是选择**最符合开源目标与风险偏好**的许可证。
- **优先推荐**：Apache-2.0（含专利授权条款，更适合多人贡献与商业友好）。
- **可选**：MIT（最简洁、宽松，但专利保护弱）。

## 五、里程碑（建议）
1. USB 投屏 + 鼠标键盘控制 + 全屏
2. 无线发现与连接 + 剪贴板同步
3. 拖拽安装 APK 与文件传输
4. 体验优化（音量恢复、通知栏、快捷操作）

